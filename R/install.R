# Install all required R packages to run the analyses
# Copyright (c), 2021, Philippe Grosjean (phgrosjean@sciviews.org)
#
# Note: we used R version 4.0.5. Results may vary with a different version of R
# We also use SciViews::R, which is an opinionated subset of R packages and
# functions on top of base R and tidyverse <https://www.tidyverse.org>
install.packages("tidyverse")
install.packages("kohonen")
